run_name: recovery
dataset: CICIDS2017
protocol: drift_window_recovery_eval
seed: 42

window:
  pre_chunks: 5
  post_chunks: 10

chunk_size: 5000
sequence_length: 10

paths:
  trace_dir: results/traces
  summary_dir: results/summaries
  figure_dir: results/figures
