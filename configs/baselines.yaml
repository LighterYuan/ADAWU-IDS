run_name: baselines
dataset: CICIDS2017
protocol: chronological_chunk_eval
seed: 42

baselines:
  - static_lstm
  - static_ensemble

chunk_size: 5000
sequence_length: 10

paths:
  trace_dir: results/traces
  summary_dir: results/summaries
  figure_dir: results/figures
  table_dir: results/tables
